{
  "name": "gold-project-root",
  "version": "1.0.0",
  "description": "Root package for Railway deployment",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.1",
    "express": "^5.2.1",
    "firebase-admin": "^13.9.0",
    "socket.io": "^4.8.3",
    "sqlite3": "5.1.7"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
